class AudioEngine { constructor() { this.ctx = null; this.masterVolume = null; this.instruments = {}; this.isInitialized = false; } init() { if (this.isInitialized) return; window.AudioContext = window.AudioContext || window.webkitAudioContext; this.ctx = new AudioContext(); this.masterVolume = this.ctx.createGain(); this.masterVolume.gain.value = 0.7; this.masterVolume.connect(this.ctx.destination); this.isInitialized = true; console.log("Audio Engine Initialized"); } setVolume(value) { if (this.masterVolume) { this.masterVolume.gain.setTargetAtTime(value / 100, this.ctx.currentTime, 0.1); } } // Professional Instrument Synthesis playNote(frequency, type = 'piano', duration = 0.5) { if (!this.ctx) this.init(); if (this.ctx.state === 'suspended') this.ctx.resume(); const now = this.ctx.currentTime; const gain = this.ctx.createGain(); gain.connect(this.masterVolume); switch (type) { case 'piano': this.createPianoSound(frequency, now, duration, gain); break; case 'drums': this.createDrumSound(frequency, now, gain); break; case 'keyboard': this.createSynthSound(frequency, now, duration, gain); break; case 'guitar': this.createGuitarSound(frequency, now, duration, gain); break; case 'violin': this.createViolinSound(frequency, now, duration, gain); break; default: this.createBasicSound(frequency, now, duration, gain); } } createDrumSound(frequency, now, gain) { // Frequency is used as a trigger ID: 60=Kick, 150=Snare, 1000=HiHat, 800=Clap if (frequency === 60) { // KICK - Deep & Punchy const osc = this.ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(150, now); osc.frequency.exponentialRampToValueAtTime(0.01, now + 0.5); gain.gain.setValueAtTime(1.5, now); gain.gain.exponentialRampToValueAtTime(0.01, now + 0.5); osc.connect(gain); osc.start(now); osc.stop(now + 0.5); } else if (frequency === 150) { // SNARE - The "Crackle" and "Body" // The Body (Tone) const osc = this.ctx.createOscillator(); osc.type = 'triangle'; osc.frequency.setValueAtTime(180, now); const oscGain = this.ctx.createGain(); oscGain.gain.setValueAtTime(0.5, now); oscGain.gain.exponentialRampToValueAtTime(0.01, now + 0.1); osc.connect(oscGain); oscGain.connect(gain); // The Snap (White Noise) const noise = this.ctx.createBufferSource(); const bufferSize = this.ctx.sampleRate * 0.2; const buffer = this.ctx.createBuffer(1, bufferSize, this.ctx.sampleRate); const data = buffer.getChannelData(0); for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1; noise.buffer = buffer; const noiseFilter = this.ctx.createBiquadFilter(); noiseFilter.type = 'highpass'; noiseFilter.frequency.value = 1000; const noiseGain = this.ctx.createGain(); noiseGain.gain.setValueAtTime(1, now); noiseGain.gain.exponentialRampToValueAtTime(0.01, now + 0.2); noise.connect(noiseFilter); noiseFilter.connect(noiseGain); noiseGain.connect(gain); osc.start(now); osc.stop(now + 0.2); noise.start(now); noise.stop(now + 0.2); } else if (frequency === 1000) { // HI-HAT const noise = this.ctx.createBufferSource(); const bufferSize = this.ctx.sampleRate * 0.1; const buffer = this.ctx.createBuffer(1, bufferSize, this.ctx.sampleRate); const data = buffer.getChannelData(0); for (let i = 0; i < bufferSize; i++) data[i] = Math.random() * 2 - 1; noise.buffer = buffer; const filter = this.ctx.createBiquadFilter(); filter.type = 'highpass'; filter.frequency.value = 7000; gain.gain.setValueAtTime(0.5, now); gain.gain.exponentialRampToValueAtTime(0.01, now + 0.05); noise.connect(filter); filter.connect(gain); noise.start(now); } else { // CLAP or generic noise gain.gain.setValueAtTime(0.8, now); gain.gain.exponentialRampToValueAtTime(0.01, now + 0.3); this.createBasicSound(frequency, now, 0.1, gain); } } createPianoSound(frequency, now, duration, gain) { const osc = this.ctx.createOscillator(); osc.type = 'sine'; gain.gain.setValueAtTime(0, now); gain.gain.linearRampToValueAtTime(0.5, now + 0.01); gain.gain.exponentialRampToValueAtTime(0.01, now + duration); osc.frequency.setValueAtTime(frequency, now); osc.connect(gain); osc.start(now); osc.stop(now + duration); } createSynthSound(frequency, now, duration, gain) { const osc = this.ctx.createOscillator(); osc.type = 'square'; const filter = this.ctx.createBiquadFilter(); filter.type = "lowpass"; filter.frequency.setValueAtTime(2000, now); filter.frequency.exponentialRampToValueAtTime(500, now + duration); osc.connect(filter); filter.connect(gain); gain.gain.setValueAtTime(0, now); gain.gain.linearRampToValueAtTime(0.2, now + 0.05); gain.gain.exponentialRampToValueAtTime(0.01, now + duration); osc.frequency.setValueAtTime(frequency, now); osc.start(now); osc.stop(now + duration); } createGuitarSound(frequency, now, duration, gain) { const osc = this.ctx.createOscillator(); osc.type = 'triangle'; gain.gain.setValueAtTime(0, now); gain.gain.linearRampToValueAtTime(0.4, now + 0.01); gain.gain.exponentialRampToValueAtTime(0.01, now + duration * 2); osc.frequency.setValueAtTime(frequency, now); osc.connect(gain); osc.start(now); osc.stop(now + duration * 2); } createViolinSound(frequency, now, duration, gain) { const osc = this.ctx.createOscillator(); osc.type = 'sawtooth'; const filter = this.ctx.createBiquadFilter(); filter.type = "lowpass"; filter.frequency.value = 1000; osc.connect(filter); filter.connect(gain); gain.gain.setValueAtTime(0, now); gain.gain.linearRampToValueAtTime(0.2, now + 0.1); gain.gain.linearRampToValueAtTime(0.01, now + duration); osc.frequency.setValueAtTime(frequency, now); osc.start(now); osc.stop(now + duration); } createBasicSound(frequency, now, duration, gain) { const osc = this.ctx.createOscillator(); osc.type = 'sine'; osc.frequency.setValueAtTime(frequency, now); osc.connect(gain); osc.start(now); osc.stop(now + duration); } } const engine = new AudioEngine(); window.audioEngine = engine;
